<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
       <encoder>
            <pattern>
                %d{dd-MM-yyyy HH:mm:ss} [%thread] %-5level %X{requestId} - %msg%n
            </pattern>
       </encoder>
    </appender>

    <logger name="practice" level="debug" additivity="false">
        <appender-ref ref="Console"/>
    </logger>

</configuration>